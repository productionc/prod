<div class="container">
<%= link_to "New Blog", new_blog_path %>
<% @blogs.each do |blog| %>
  <h3><%= blog.title.html_safe %></h3>
  <p><%= blog.body.html_safe %></p>
  <%= link_to "Edit blog", edit_blog_path(blog) %>
  <% if blog != @blogs.last %>
  <hr />
  <% end %>
<% end %>
</div>