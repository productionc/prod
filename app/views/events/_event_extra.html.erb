 <div class="field">
  <%= f.label "looking for" %><br />
  <%= f.collection_check_boxes :event_extra_ids, EventExtra.all, :id, :name do |b| %>
    <div class="collection-check-box">
      <%= b.check_box %>
      <%= b.label %>
    </div>
  <% end %>
  </div>